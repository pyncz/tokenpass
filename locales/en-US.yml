logo: '{name} logo'

# meta
pages:
  index:
    title: Setup
    description:
      Using the unique power of blockchain.
      Setup collection contract on any EVM blockchain, let your customer
      read the generated QR Code and check if he ownes the specified token.
  about:
    title: About
    description: Why may you need tokenpass?
  settings:
    title: Settings

theme:
  system: System
  light: Light
  dark: Dark
  black: Black

colorMode: Color mode
locale: Localization
madeBy: Made by
home: Home

# tokens' amounts
inUnits: In units
formatted: Formatted

# buttons' actions
actions:
  setup: Setup
  use: Use
  close: Close
  copy: Copy to clipboard

status:
  pending: Pending
  checking: Checking
  initializing: Initializing
  initialized: Initialized
  generating: Generating connection link

fields:
  chainId: Chain ID
  collectionAddress: Collection’s contract address
  tokenId: Token ID
  amount: Amount

tooltips:
  test: Test network
  initializing: WalletConnect AuthClient

errors:
  noConnection: No connection
  noCollection: There's no such collection. Check the selected address / chain
  notFound: Not found

validations:
  required: This field is required
  integer: The value should be an integer
  positive: The value should be a positive number
  ethAddress: An eth address is expected

contractCheck:
  noCode: Not a contract!
  unknown: Unknown contract interface!
  type: Looks like this is an {type}!
  disclaimer:
    Check the selected chain and address, and specify an
    [ERC-721](https://eips.ethereum.org/EIPS/eip-721),
    [ERC-1155](https://eips.ethereum.org/EIPS/eip-1155)
    or [ERC-20](https://eips.ethereum.org/EIPS/eip-20)
    contract so we can check if your client owns the token.

result:
  passed:
    title: Passed
  failed:
    title: Failed
  attributes:
    amount: Required amount
    balance: Current balance

feedback:
  copied: Copied!

# by pages
index:
  title:
    setup: Setup address
    share: Share link
  submit: '@:actions.setup'
  disclosure: Additional config
  fields:
    chainId:
      label: Chain
      placeholder: Select chain ID
    address:
      label: '@:fields.collectionAddress'
      placeholder: 0xABC...123
    tokenId:
      label: Look for a specific token
      placeholder: Token ID
    amount:
      label: Look for a specific amount (1 if not specified)
      placeholder: Amount
      placeholderUnits: Amount (in units)
      placeholderFormatted: Amount (formatted)
      description:
        ERC1155: Amount of tokens of the provided `tokenId`
        ERC20: In units, e.g. `{units}` for 1 {symbol}

about:
  title: About
