logo: "{name} logo"

# meta
pages:
  index:
    title: Setup
  check:
    title: 'Check #{id}'
  about:
    title: About
  settings:
    title: Settings

theme:
  system: System
  light: Light
  dark: Dark
  black: Black

colorMode: Color mode
locale: Localization

# buttons' actions
actions:
  setup: Setup

fields:
  chainId: Chain ID
  collectionAddress: Collection’s contract address
  tokenId: Token ID
  amount: Amount

# by pages
setup:
  title: Setup address
  submit: '@:actions.setup'
  fields:
    chainId:
      label: Chain
      placeholder: Select chain ID
    address:
      label: '@:fields.collectionAddress'
      placeholder: 0xABC...123
    tokenId:
      label: Look for a specific token
      placeholder: Token ID
    amount:
      label: Look for a specific amount
      placeholder: Amount
